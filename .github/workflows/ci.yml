name: CI

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]

env:
  GO_VERSION: '1.24'

jobs:
  # --- Проверки кода: vet, build, test ---
  check:
    name: Lint, vet, build, test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: go.sum

      - name: Download dependencies
        run: go mod download

      - name: Verify dependencies
        run: go mod verify

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v4
        with:
          version: latest

      - name: Vet
        run: go vet ./...

      - name: Build
        run: go build -v -o feeder-server ./cmd/graph

      - name: Test
        run: go test -v -race -count=1 ./...

  # --- Docker build (проверка что образ собирается) ---
  docker:
    name: Docker build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build backend image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: false
          load: true
          tags: cnpf-feeder-backend:ci

  # --- Placeholder для PDP deploy (раскомментировать когда будет готово) ---
  # deploy-pdp:
  #   name: Deploy PDP
  #   needs: [check, docker]
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main'
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v4
  #     # Добавить шаги деплоя на ваш хостинг (Railway, Fly.io, VPS и т.д.)
  #     # - name: Deploy
  #     #   run: ...
