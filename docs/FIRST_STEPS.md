# Первые шаги в GraphQL Playground

## 🎯 Что делать прямо сейчас

### Шаг 1: Удалите весь текст в редакторе

В правой панели (где много текста с комментариями `#`) - удалите всё и оставьте пустое поле.

### Шаг 2: Вставьте этот простой запрос

```graphql
{
  __typename
}
```

### Шаг 3: Нажмите кнопку Play (▶️) или `Ctrl+Enter`

Вы должны увидеть результат:
```json
{
  "data": {
    "__typename": "Query"
  }
}
```

**Если увидели этот результат - всё работает! ✅**

---

## 📝 Ваш первый реальный запрос

После проверки, попробуйте получить список отчетов:

```graphql
{
  reports(limit: 5) {
    id
    title
    text
    author {
      username
    }
  }
}
```

Нажмите Play (▶️) - вы увидите список отчетов (может быть пустым, если база пустая).

---

## 🔐 Регистрация пользователя

Чтобы зарегистрироваться, вставьте:

```graphql
mutation {
  register(input: {
    email: "test@example.com"
    username: "testuser"
    password: "password123"
    passwordConfirm: "password123"
  }) {
    ok
    token
  }
}
```

**Важно:** После регистрации токен сохранится автоматически в cookie, и вы сможете выполнять авторизованные запросы.

---

## 👤 Проверка текущего пользователя

После регистрации, проверьте себя:

```graphql
{
  me {
    id
    email
    username
    isAdmin
  }
}
```

---

## 📊 Создание отчета

После регистрации, создайте отчет:

```graphql
mutation {
  createReport(input: {
    title: "Мой первый отчет"
    text: "Это тестовый отчет для проверки"
  }) {
    id
    title
    text
    createdAt
  }
}
```

---

## ⚠️ Про ошибку "Introspection failed"

**Не переживайте!** Эта ошибка не мешает работе. Она означает, что автодополнение может работать не идеально, но запросы выполняются нормально.

Если хотите убрать ошибку, добавьте в вкладку "Headers" (внизу):

```json
{
  "Content-Type": "application/json"
}
```

Но это не обязательно для начала работы.

---

## 🎯 Быстрая шпаргалка

1. **Удалите весь текст** в редакторе
2. **Вставьте запрос** (например, `{ __typename }`)
3. **Нажмите Play** (▶️) или `Ctrl+Enter`
4. **Смотрите результат** справа

---

## 💡 Полезные кнопки

- **▶️ Play** - выполнить запрос
- **📋 Prettify** - красиво отформатировать запрос
- **🔄** - обновить схему (может помочь с ошибкой introspection)

---

## 📚 Больше примеров

Смотрите: [GRAPHQL_EXAMPLES.md](./GRAPHQL_EXAMPLES.md)
