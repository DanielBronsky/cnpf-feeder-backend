# Быстрое тестирование GraphQL API

## 🚀 Быстрый старт

### 1. Откройте GraphQL Playground

Откройте в браузере: **http://localhost:4000/**

### 2. Первый запрос - проверка работы

Вставьте в левую панель и нажмите "Play":

```graphql
query {
  __typename
}
```

**Ожидаемый результат:**
```json
{
  "data": {
    "__typename": "Query"
  }
}
```

### 3. Регистрация пользователя

```graphql
mutation {
  register(input: {
    email: "test@example.com"
    username: "testuser"
    password: "password123"
    passwordConfirm: "password123"
  }) {
    ok
    token
  }
}
```

**После регистрации:**
- Токен сохранится в cookie автоматически
- Вы сможете выполнять авторизованные запросы

### 4. Проверка текущего пользователя

```graphql
query {
  me {
    id
    email
    username
    isAdmin
  }
}
```

### 5. Получить список отчетов

```graphql
query {
  reports(limit: 10) {
    id
    title
    text
    createdAt
    author {
      username
    }
    photos {
      url
    }
  }
}
```

### 6. Создать отчет

```graphql
mutation {
  createReport(input: {
    title: "Мой первый отчет"
    text: "Это тестовый отчет для проверки API"
  }) {
    id
    title
    text
    createdAt
    author {
      username
    }
  }
}
```

## 📋 Полный список примеров

Смотрите: [GRAPHQL_EXAMPLES.md](./GRAPHQL_EXAMPLES.md)

## 💡 Полезные функции Playground

1. **Автодополнение** - нажмите `Ctrl+Space` для подсказок
2. **Документация схемы** - вкладка "Schema" справа
3. **История запросов** - вкладка "History"
4. **Переменные** - вкладка "QUERY VARIABLES" внизу
5. **HTTP Headers** - для авторизации через Bearer token

## 🔍 Проверка через терминал

### Простой запрос
```bash
curl -X POST http://localhost:4000/graphql \
  -H "Content-Type: application/json" \
  -d '{"query":"{ __typename }"}'
```

### С авторизацией (через cookie)
```bash
# Сначала залогиньтесь через Playground, затем:
curl -X POST http://localhost:4000/graphql \
  -H "Content-Type: application/json" \
  -H "Cookie: cnpf_auth=YOUR_TOKEN" \
  -d '{"query":"{ me { id username } }"}'
```

## ⚠️ Частые проблемы

1. **"Не авторизован"** → Сначала выполните `register` или `login`
2. **Пустой список отчетов** → Это нормально, если база пустая. Создайте отчет через `createReport`
3. **Ошибка валидации** → Проверьте формат данных (email, длина пароля и т.д.)

## 🎯 Рекомендуемый порядок тестирования

1. ✅ Проверка работы: `{ __typename }`
2. ✅ Регистрация: `register`
3. ✅ Проверка пользователя: `me`
4. ✅ Создание отчета: `createReport`
5. ✅ Получение отчетов: `reports`
6. ✅ Обновление отчета: `updateReport`
7. ✅ Удаление отчета: `deleteReport`
