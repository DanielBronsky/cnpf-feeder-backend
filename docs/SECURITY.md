# Безопасность

## Аутентификация

### JWT Токены
- Используется библиотека `golang-jwt/jwt/v5` для создания и проверки JWT токенов
- Алгоритм: HS256
- Срок действия: 30 дней
- Токен хранится в httpOnly cookie `cnpf_auth`
- Секретный ключ: `AUTH_SECRET` (должен быть длинным случайным строкой)

### Пароли
- Пароли хранятся в базе данных как bcrypt hash (никогда в открытом виде)
- При логине пароль передается в GraphQL запросе в открытом виде
- **ВАЖНО**: Это нормально только при использовании HTTPS

## HTTPS в Production

### ⚠️ КРИТИЧЕСКИ ВАЖНО

**В production окружении ОБЯЗАТЕЛЬНО использовать HTTPS!**

Без HTTPS:
- Пароли передаются в открытом виде по сети
- Любой может перехватить пароли через MITM атаку
- JWT токены могут быть перехвачены

### Настройка HTTPS

#### Вариант 1: Reverse Proxy (Nginx/Traefik)
```nginx
server {
    listen 443 ssl;
    server_name your-domain.com;

    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;

    location / {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

#### Вариант 2: Docker с SSL сертификатами
```yaml
services:
  frontend:
    # ... 
    environment:
      - NODE_ENV=production
    # Используйте reverse proxy для HTTPS
```

#### Вариант 3: Cloud Provider (Vercel, AWS, etc.)
- Большинство облачных провайдеров предоставляют HTTPS автоматически
- Убедитесь, что используете HTTPS endpoints

### Проверка HTTPS

После настройки проверьте:
1. Все запросы идут через `https://` (не `http://`)
2. В браузере есть замочек 🔒 рядом с URL
3. Cookie установлены с флагом `Secure` (уже настроено в коде)

## Что видно в Network Tab

### Development (localhost)
- ✅ Пароль виден в GraphQL запросе (это нормально для localhost)
- ⚠️ Используется HTTP (не HTTPS) - это нормально только для разработки

### Production
- ✅ Пароль виден в GraphQL запросе (это нормально при HTTPS)
- ✅ HTTPS шифрует весь трафик между браузером и сервером
- ✅ Даже если кто-то перехватит пакеты, они будут зашифрованы

## Защита от атак

### 1. Rate Limiting
Рекомендуется добавить rate limiting для защиты от brute-force атак:
- Ограничить количество попыток входа с одного IP
- Использовать библиотеку типа `golang.org/x/time/rate`

### 2. CORS
- CORS настроен в `cmd/graph/server.go`
- Разрешен только указанный `CORS_ORIGIN`

### 3. Cookie Security
- `httpOnly: true` - JavaScript не может прочитать cookie
- `secure: true` в production - cookie передается только по HTTPS
- `sameSite: 'lax'` - защита от CSRF атак

### 4. Пароли
- Минимальная длина: 8 символов
- Максимальная длина: 72 символа (ограничение bcrypt)
- Хранятся как bcrypt hash (никогда в открытом виде)

## Рекомендации

1. **Всегда используйте HTTPS в production**
2. **Никогда не логируйте пароли** (уже соблюдается)
3. **Используйте сильный AUTH_SECRET** (минимум 32 символа)
4. **Регулярно обновляйте зависимости** для исправления уязвимостей
5. **Добавьте rate limiting** для защиты от brute-force
6. **Мониторьте подозрительную активность** (множественные неудачные попытки входа)

## Проверка безопасности

Перед деплоем в production проверьте:
- [ ] HTTPS настроен и работает
- [ ] Cookie установлены с `Secure: true`
- [ ] `AUTH_SECRET` - длинная случайная строка
- [ ] Пароли не логируются
- [ ] CORS настроен правильно
- [ ] Rate limiting добавлен (рекомендуется)
